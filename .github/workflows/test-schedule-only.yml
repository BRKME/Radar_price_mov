name: Test Schedule ONLY
on:
  schedule:
    - cron: '*/10 * * * *'  # Каждые 10 минут

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test schedule
        run: |
          echo "🟢 SCHEDULE TEST WORKFLOW"
          echo "🕐 Time: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          echo "📅 This runs ONLY on schedule"
          echo "✅ If you see this, schedule works!"
          echo "Event: ${{ github.event_name }}"
